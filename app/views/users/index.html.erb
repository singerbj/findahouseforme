<h1>Users</h1>
<%= will_paginate %>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.username %>
      <% if user.admin = true %>
        (Admin)
      <% end %>  
      <%= link_to "Toggle Admin", edit_user_path(user) %>
    </li>   
  <% end %>
</ul>
<%= will_paginate %> 
